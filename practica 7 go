package main

import "fmt"

// Función que recibe dos enteros y devuelve su suma
func suma(a int, b int) int {
	return a + b
}

func main() {
	resultado1 := suma(5, 3)
	fmt.Println("La suma de 5 y 3 es", resultado1)

	resultado2 := suma(10, 7)
	fmt.Println("La suma de 10 y 7 es", resultado2)
}
